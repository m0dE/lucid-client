import importerSelectors from 'src/modules/shared/importer/importerSelectors';

const userSettingsImporterSelectors = importerSelectors(
  'userSettings.importer',
);

export default userSettingsImporterSelectors;
